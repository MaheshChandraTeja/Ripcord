<Page
    x:Class="Ripcord.App.Views.FreeSpacePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="8" Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Controls -->
        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBox Width="360" Header="Any path on target volume" x:Name="PathBox" PlaceholderText="e.g., C:\" />
            <NumberBox Width="180" Header="Reserve (MB)" x:Name="ReserveBox" Value="1024" Minimum="0" SmallChange="128" LargeChange="1024"/>
            <Button Content="Analyze" Click="Analyze_Click"/>
            <Button Content="Start Wipe" x:Name="StartBtn" Click="Start_Click" IsEnabled="False"/>
            <Button Content="Cancel" x:Name="CancelBtn" Click="Cancel_Click" IsEnabled="False"/>
        </StackPanel>

        <!-- Status -->
        <StackPanel Grid.Row="1" Spacing="6">
            <TextBlock x:Name="CapsText" TextWrapping="Wrap" />
            <ProgressBar x:Name="Progress" Minimum="0" Maximum="100" />
            <TextBlock x:Name="ProgressText" />
        </StackPanel>

        <TextBlock Grid.Row="2" Text="Tip: Free-space wipe allocates most free space with zeroes, issues TRIM where supported, then deletes the temporary files."/>
    </Grid>
</Page>
