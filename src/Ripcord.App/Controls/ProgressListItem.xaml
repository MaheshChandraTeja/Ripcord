<UserControl
    x:Class="Ripcord.App.Controls.ProgressListItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cvt="using:Ripcord.App.Converters">

    <UserControl.Resources>
        <cvt:BytesToStringConverter x:Key="BytesConv"/>
        <cvt:PercentConverter x:Key="PctConv"/>
    </UserControl.Resources>

    <Grid Padding="8" ColumnSpacing="12" RowSpacing="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="160"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Bind FilePath, Mode=OneWay}" TextTrimming="CharacterEllipsis"/>
        <TextBlock Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Text="{x:Bind Status, Mode=OneWay}"/>

        <ProgressBar Grid.Row="1" Grid.ColumnSpan="2" Minimum="0" Maximum="100" Value="{x:Bind Percent, Mode=OneWay}" Height="6"/>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <TextBlock>
                <Run Text="{x:Bind BytesProcessed, Converter={StaticResource BytesConv}}"/>
                <Run Text=" / "/>
                <Run Text="{x:Bind BytesTotal, Converter={StaticResource BytesConv}}"/>
            </TextBlock>
            <TextBlock Text="{x:Bind Percent, Converter={StaticResource PctConv}}"/>
        </StackPanel>
    </Grid>
</UserControl>
